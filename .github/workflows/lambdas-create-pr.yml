name: Validate Terraform, Run Tests, and Create Pull Request

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'src/**'
      - 'tests/**'
      - 'infra/**'
      - '.github/config/variables-lambda.yml'
      - '.github/workflows/lambdas-create-pr.yml'
      - '.github/workflows/lambdas-deploy.yml'

jobs:
  load-variables:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Load variables from YAML
        id: export-vars
        run: |
          echo "Exporting variables from YAML to $GITHUB_ENV..."

          # AWS and Terraform variables
          echo "AWS_REGION=$(yq '.github_env.aws.region' .github/config/variables-lambda.yml)" >> $GITHUB_ENV
          echo "TERRAFORM_DIRECTORY=$(yq '.terraform.directory' .github/config/variables-lambda.yml)" >> $GITHUB_ENV
          echo "LAMBDA_RUNTIME=$(yq '.lambda.runtime' .github/config/variables-lambda.yml)" >> $GITHUB_ENV

          # Lambda configurations
          lambdas=$(yq -o=json '.lambda.lambdas' .github/config/variables-lambda.yml | jq -c '.')
          echo "LAMBDAS=$lambdas" >> $GITHUB_ENV

          # Test configurations
          test_lambdas=$(yq -o=json '.test.lambdas' .github/config/variables-lambda.yml | jq -c '.')
          echo "TEST_LAMBDAS=$test_lambdas" >> $GITHUB_ENV

          # Toggle and coverage
          echo "TEST_RUN_TOGGLE=$(yq '.test.run-toogle' .github/config/variables-lambda.yml)" >> $GITHUB_ENV
          echo "COVERAGE_FAIL_UNDER=$(yq '.test.coverage.fail_under' .github/config/variables-lambda.yml)" >> $GITHUB_ENV

          # Secrets
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
          echo "GITHUB_PAT=${{ secrets.PAT }}" >> $GITHUB_ENV

  check-variables:
    runs-on: ubuntu-latest
    needs: load-variables

    steps:
      - name: Validate & Print JSON Outputs
        run: |
          echo "Validating JSON Outputs..."

          if [[ -z "$LAMBDAS" ]]; then
            echo "Error: LAMBDAS variable is empty or not defined."
            exit 1
          fi
          echo "Lambdas Config JSON:"
          echo "$LAMBDAS" | jq .

          if [[ -z "$TEST_LAMBDAS" ]]; then
            echo "Error: TEST_LAMBDAS variable is empty or not defined."
            exit 1
          fi
          echo "Test Lambdas Config JSON:"
          echo "$TEST_LAMBDAS" | jq .

      - name: Validate & Print All Outputs
        run: |
          echo "Validating and Printing All Outputs..."

          vars=("AWS_REGION" "TERRAFORM_DIRECTORY" "LAMBDA_RUNTIME" "LAMBDAS" "TEST_LAMBDAS" "TEST_RUN_TOGGLE" "COVERAGE_FAIL_UNDER")
          for var in "${vars[@]}"; do
            if [[ -z "${!var}" ]]; then
              echo "Error: $var is empty or not defined."
              exit 1
            else
              echo "$var: ${!var}"
            fi
          done

  terraform-validate:
    runs-on: ubuntu-latest
    needs: load-variables

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        run: terraform init
        working-directory: $TERRAFORM_DIRECTORY

      - name: Terraform Validate
        run: terraform validate
        working-directory: $TERRAFORM_DIRECTORY

  run-tests:
    runs-on: ubuntu-latest
    needs: terraform-validate

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: $LAMBDA_RUNTIME

      - name: Install Dependencies for All Test Directories
        run: |
          echo "Processing test lambdas configuration..."
          echo "Raw TEST_LAMBDAS variable:"
          echo "$TEST_LAMBDAS"

          if [[ -z "$TEST_LAMBDAS" ]]; then
            echo "Error: TEST_LAMBDAS is empty."
            exit 1
          fi

          test_lambdas=$(echo "$TEST_LAMBDAS" | jq -c '.[]')
          for test_lambda in $test_lambdas; do
            name=$(echo $test_lambda | jq -r '.name')
            source=$(echo $test_lambda | jq -r '.source')

            if [[ -z "$name" || -z "$source" ]]; then
              echo "Error: Invalid test lambda configuration."
              exit 1
            fi

            if [ -f "$source/requirements.txt" ]; then
              echo "Installing dependencies from $source/requirements.txt"
              pip install -r "$source/requirements.txt" || exit 1
            else
              echo "No requirements.txt found for $name"
            fi
          done

      - name: Run All Tests and Generate Coverage Report
        run: |
          coverage run -m unittest discover -s tests
          coverage report -m --fail-under=$COVERAGE_FAIL_UNDER
          coverage html
          coverage-badge -o coverage.svg -f

      - name: Organize Coverage Reports
        run: |
          mkdir -p tests/reports
          mv htmlcov tests/reports/htmlcov
          mv coverage.svg tests/reports/coverage.svg

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: tests/reports/htmlcov

      - name: Upload Coverage Badge
        uses: actions/upload-artifact@v4
        with:
          name: coverage-badge
          path: tests/reports/coverage.svg

  sync-develop-to-main:
    runs-on: ubuntu-latest
    needs: [terraform-validate, run-tests]
    if: success()

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Sync develop to main
        run: |
          git fetch origin
          git checkout develop
          git pull origin develop
          git fetch origin ${{ github.event.pull_request.head.ref }}:${{ github.event.pull_request.head.ref }}

      - name: Merge PR to Develop
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git merge --no-ff ${{ github.event.pull_request.head.ref }}
          git push origin develop
